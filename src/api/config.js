// 用户模块的endpoint
export const user = {
  auth: "user/auth",
  create: "user/create",
  delete: "user/delete",
  update: "user/update",
  select: "user/select",
};

export const equiments = {
  select: "equiments/select",
  all: "equiments/all",
};
